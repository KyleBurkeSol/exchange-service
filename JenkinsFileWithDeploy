pipeline {
    parameters {
        string( name: 'pcfToken', description: 'CF access token used to deploy the app')
    }
    agent any
    stages {
        stage('Clean') {
            steps {
                sh './gradlew clean'
            }
        }
        stage('Build') {
            steps {
                sh './gradlew assemble'
            }
        }
        stage('Test') {
            steps {
                sh 'echo "skip tests for now"'
            }
        }
        stage('Deploy') {
            steps {
                sh 'python -u deploy.py ${pcfToken}'
            }
        }
    }
}
